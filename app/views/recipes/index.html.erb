<h1>Recipes</h1>

pages:
<%= render "recipes/paginate", pages_count: @pages_count, current_page: @page %>

<hr>

<% @recipes.each do |recipe| %>
  <%= link_to(recipe_path(recipe.id)) do %>
    <%= recipe.title %><br>
    <%= image_tag(recipe.image_url, class: 'image_container') %><br>
  <% end %>
  <hr>
<% end %>

<br>

<hr>

pages:
<%= render "recipes/paginate", pages_count: @pages_count, current_page: @page %>
